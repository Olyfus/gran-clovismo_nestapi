import {CourseObj} from "./course.object";
import {CasesObj} from "./case.object";

export class MapObj {
  private _id !: number;
  private _nomMap !: string;
  private _nomFichier !: string;
  private _cases !: CasesObj[];
}
